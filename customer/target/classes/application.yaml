server:
  port: 8080
spring:
  application:
    name: customer
  datasource:
    password: secret
    url: jdbc:postgresql://localhost:6432/customer
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

